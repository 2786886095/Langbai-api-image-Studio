# AI 图片生成器 v1.6.33

## 全错误多轮重试

- “失败后追加次数”现在对所有错误生效，包括内容审核失败、401、400、404 和 HTTP 504。
- 每个失败项会执行完整设定轮次；一旦成功返回真实图片，该项立即停止重试。
- 瞬时网络错误继续使用退避等待，其它错误在各轮之间保留短暂间隔，避免瞬间连续请求。

## 补充剩余失败修复

- 修复“补充剩余失败”点击后没有反应的问题。
- 已经耗尽本轮次数但仍然失败的卡片可以重新加入，并从第 1 轮重新计算设定次数。
- 同时收集未被当前队列跟踪的新失败项。
- 正在请求或已经排队的卡片不会被重复加入，进度与成功/失败计数会同步修正。

## 风险说明

- 本行为是用户明确授权的重复提交。审核不通过、无效密钥或无效参数通常不会因为重复请求自行恢复。
- HTTP 504 的任务结果可能未知，多轮重提可能产生重复任务或重复计费。

## 验证

- 浏览器完整交互回归覆盖审核失败和 504 的完整轮次、成功即停、耗尽卡片重启与遗漏失败收集。
- 静态审计、代理与网关测试、`flutter analyze` 和 Flutter 全量测试通过。
- Gemini 本地去水印、API 配置、账号、历史项目、缓存与导出保持不变。
