# AI 图片生成器 v1.6.30

## 内容策略失败重试修复

- 修复 HTTP 400 内容策略失败后，“重试”按钮可见但点击没有响应的问题。
- 错误分类现在同时识别 `content policy`、`content policies` 及常见的提示词违规响应。
- 单图与漫画分镜任务点击重试后会自动打开提示词编辑框；确认修改后才重新提交。
- 漫画内容策略重试只使用修改后的当前分镜提示词，避免旧的全局提示词再次触发同一错误。

## 重试策略

- 单卡手动重试与批量原样重试分别控制。
- 需要修改提示词或参考图的失败卡片仍会从“全部重试”中排除，防止原参数被批量重复提交。
- 共用前端重试逻辑对单图、漫画分镜及各图像接口配置统一生效。

## 验证

- JavaScript 语法检查、静态审计和 Android 资源一致性检查通过。
- 图片任务稳定性测试 13 项通过，完整浏览器回归通过。
- `flutter analyze` 通过；Flutter 测试 81 项通过，1 项打包进程环境测试按条件跳过。
