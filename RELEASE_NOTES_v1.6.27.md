# AI 图片生成器 v1.6.27

## ChatGPT 网页生图并发设置

- 最大并发现在可由用户在 `1–100` 间自行调整，默认值为 `5`。
- 设置会随当前 API 配置、已保存配置和独立恢复偏好持久化，软件重启后继续使用最后选择的数值。
- Windows 内置网关与 Android 网关均开放到 100，旧版网关配置不会再把软件中的选择静默截断为 3。
- 运行时熔断与故障降速继续生效；高并发对 ChatGPT 账号会话和网络稳定性的要求更高。

## 保留的稳定性修复

- 同账号令牌激活继续使用单飞机制，避免批量任务重复冲击会话桥。
- accessToken 临近到期或出现 HTTP 401 时，优先静默刷新原账号并重试当前图片。
- Windows 与 Android 在任务成功、失败或取消后继续清理 ChatGPT 临时图片对话。

## 验证

- JavaScript 语法、适配器测试、静态审计和 Python 会话清理回归通过。
- 完整浏览器回归通过，覆盖输入 100、保存配置和重启恢复。
- `flutter analyze` 通过；Flutter 测试 81 项通过，1 项打包进程环境测试按条件跳过。
