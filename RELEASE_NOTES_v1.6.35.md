# AI 图片生成器 v1.6.35

## GrsAI 模型目录更新

- 修复“检测”按钮只重新加载固定 13 个模型、未查询在线目录的问题。
- 点击模型旁的“检测”会联网刷新 GrsAI 生图模型，只收录图片模型，不混入文本或视频模型。
- 内置名单新增 `gpt-image-2.5`、`gpt-image-2.5-sunburst`、`gpt-image-2.5-flare`、`nano-banana-2-lite`；更新后仍可手动输入模型 ID。
- 保留当前选择和已保存的旧模型，不再因刷新名单清空模型输入。
- 查询结果保存在独立模型目录缓存，离线时使用缓存或带日期的内置名单，并明确提示刷新失败。
- 查询中切换 API 配置时，迟到的结果不会覆盖其他供应商的模型列表。
- GrsAI 价格改为展示在线目录返回的积分，不再沿用过期的固定人民币报价；实际价格以平台计费为准。

## 保留已有功能

- 不清空 API 配置、账号、密钥、历史项目和图片缓存。
- 保留 v1.6.34 文件夹/ZIP 导出完整性修复。
- Gemini 自动去水印、导入图片去水印与引擎资源保持不变。
- Android 内嵌前端资源同步更新。

## 使用方法

升级后选择 **GrsAI 生图 API**，点击“模型”旁边的 **检测**，再选择新模型。
模型目录来自 GrsAI 网站当前公开查询接口；该接口变动或网络中断时仍保留已有名单。
目录出现某个模型并不等于你的账号必然拥有该模型的调用权限。

## 下载

- Windows：`AI-Image-Generator-Setup.exe`
- Android：`app-release.apk`
- macOS：`AI-Image-Generator-macos.zip`
- iOS：`AI-Image-Generator-ios-unsigned.zip`（未签名，需自行签名后安装）
- 完整性校验：`SHA256SUMS.txt`
