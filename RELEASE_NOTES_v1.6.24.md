# AI 图片生成器 v1.6.24

## ChatGPT 网页生图配置持久化

- 质量和尺寸处理选项现在同时写入活动配置、已保存配置与独立恢复偏好。
- 软件重启后恢复用户最后选择，不再回落到“中 / 精确输出”。
- 重新选择已保存的 ChatGPT 网页生图配置时，继续使用该配置最后保存的质量和尺寸处理方式。
- 旧版缺少相关字段的 ChatGPT 配置会使用独立恢复偏好兼容恢复。

## ChatGPT 网页生图网关稳定性

- 健康检查失败时在结果中显示实际原因，不再只显示笼统的英文网关提示。
- 对可恢复的本地网关故障自动重启一次并重新执行健康与能力检查。
- 同一批并发 502/503 只累计为一次熔断失败，减少短暂上游抖动造成的频繁熔断。
- 熔断期间显示恢复倒计时；倒计时结束后自动检测网关，仍未恢复时继续按 15 秒间隔复检。

## 验证与兼容

- 新增“改值 → 重启 → 恢复 → 旧配置恢复”浏览器回归。
- 完整浏览器回归覆盖嵌字自定义模板、Gemini 本地去水印、历史记录、项目导入导出及多 API 配置。
- 保留 v1.6.23 的 Gemini 自动去水印、批量导入和自选路径保存功能。
- 保留已有 API 配置、账号、历史项目、缓存和下载目录。
