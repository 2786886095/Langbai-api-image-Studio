# AI 图片生成器 v1.7.0

版本：`1.7.0+113` · Studio 界面重构

## 新界面

- 顶部按创作、项目历史、API / 账号、设置分区，不再把全部功能挤在左侧。
- 保留蓝白浅色、低亮黑紫深色；移动端区分创作参数和分镜结果，优化滚动与触控尺寸。
- 五语言的蒙版、账号、分镜与缓存文案补齐，切换语言不再误改用户提示词。
- 删除分镜支持撤销；历史搜索支持中文输入法组合输入与防抖。

## 大项目与图片可靠性

- 历史、结果每页最多显示 60 张卡片；搜索、全部重试、补充失败、导出和保存仍覆盖所有页。
- 历史卡片按记录复用；缩略图按可见区域加载，最多 4 路，关闭历史时释放预览资源。
- 本地缓存失败后保留已取得的原图，支持只重试本地缓存或直接导出，不重新提交生图。
- 修复历史缓存写入失败后重复请求过期图片 URL 的丢图问题。
- 保留既有 ZIP / 文件夹导出完整性校验、项目恢复与全部失败重试逻辑。

## 启动与兼容

- 启动前先检查 WebView 能力；引擎过旧时显示可读的升级提示，不再留下按钮失效的半启动界面。
- 保留 Windows 现有 WebView2 宿主与账号数据路径，更新无需清空配置。
- 启用主页面 CSP，保留本地图片、Blob worker 和自定义 API 请求；不加载外部字体。
- 前端打包校验包含启动脚本、新界面资源、供应商适配器和去水印引擎。

## 保留功能

- OpenAI、GrsAI、ChatGPT、Gemini、OpenCodex 与自定义 API 的既有入口及配置保存。
- GrsAI 在线模型目录、参考图、蒙版重绘、历史项目和自动缓存。
- **Gemini 自动去水印与导入图片去水印保留，去水印引擎文件未改。**

## 下载与验证范围

- Windows：`AI-Image-Generator-Setup.exe`
- Android：`app-release.apk`（沿用原发布签名）
- macOS：`AI-Image-Generator-macos.zip`
- iOS：`AI-Image-Generator-ios-unsigned.zip`（未签名，需要自行签名安装）
- 文件校验：`SHA256SUMS.txt`

发布验收包括自动回归、四端构建、包内前端资源检查及 Windows 主窗口启动/控件自检。
响应式浏览器测试和 CI 构建不等于所有型号的手机、平板与系统版本均已真机验收。
