# AI 图片生成器 v1.6.26

## ChatGPT 网页账号稳定性

- 同一账号、同一令牌的并发激活合并为一次，70 张等大批量任务不会重复冲击本机会话桥。
- ChatGPT 网页生图并发上限收紧为 3；旧配置中的 10、70、100 等数值会自动按 3 执行。
- 同时记录浏览器会话和 accessToken 两种到期时间，并采用更早者；到期前 5 分钟会用原账号 WebView2 配置静默刷新。
- 遇到 HTTP 401 时先静默刷新原账号并重试当前图片；刷新结果仍失效后再进入账号切换流程。

## 临时对话清理

- Windows 网关在任务成功、失败或取消后隐藏对应 ChatGPT 临时对话。
- Android 网关在取得本地图片字节或任务退出后隐藏对应临时对话。
- 取消接口现在返回 `cancelled`，队列中尚未开始的任务不会再创建网页对话。

## 验证

- 新增 70 次并发账号激活单飞回归。
- 新增桌面成功、失败、取消清理回归与 Android 清理契约回归。
- 新增 accessToken/浏览器 Cookie 到期时间优先级回归。
