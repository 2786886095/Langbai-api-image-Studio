# AI 图片生成器 v1.6.1

## 本次新增

- Gemini 网页生图改为 **Windows、Android、macOS、iOS 软件内登录**。
- 登录成功后登录界面自动收起，后续任务由隐藏浏览器继续执行。
- 支持保存多个本机 Gemini 账号、手动切换，以及额度不足或登录失效后的自动切换。
- 每个账号使用独立本地会话；删除账号时同时清理对应的安全会话或 WebView2 数据目录。
- Gemini 任务支持临时对话、参考图、任务检查点、重启续传、真实尺寸审计、精确输出与本地 4K 后处理。

## 缓存与保存

- 生图成功后立即进入软件本地缓存，避免网页图片地址失效。
- 只有点击下载、导出 ZIP 或保存到文件夹时，才写入用户选择的输出目录。
- 登录 Cookie、内部连接密钥不会写入历史记录、项目文件或 ZIP。

## 修复与保护

- Gemini、ChatGPT、官方 OpenAI、GrsAI、自定义 API 的账号和配置继续完全隔离。
- 修复隐藏浏览器启动期间错误提示“没有可用账号”的问题；任务会等待对应账号浏览器就绪。
- Android 切换 Gemini 账号时只处理 Google/Gemini Cookie，不清空 ChatGPT 等其他内置网页登录。
- 额度或登录失效的自动切号只重排当前任务，并记录已经尝试过的账号，避免账号间循环。
- 移除旧 Gemini Chromium 扩展与伴侣 ZIP，不再要求安装扩展。

## 已完成验证

- JavaScript 静态审计、代理与适配器测试通过。
- 浏览器完整交互回归通过。
- Flutter Analyze 通过。
- Flutter 测试 44 项通过，另有 1 项按环境跳过。
- Android debug 与 release APK 本地实际构建通过。
- Windows、macOS、iOS 最终包由 GitHub Actions 四端 CI 编译验证。

## 使用边界

Gemini 网页结构、Google 登录策略、地区、账号风控、审核和图片额度均可能变化。若 Google 阻止嵌入式 OAuth 或页面结构发生变化，软件会显示错误并需要升级适配；本版本不再回退到浏览器扩展。
