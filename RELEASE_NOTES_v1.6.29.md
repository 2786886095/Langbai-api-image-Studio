# AI 图片生成器 v1.6.29

## 技能区默认折叠

- 左侧“当前模式技能”区域现在默认折叠，让单图和漫画分镜编辑区保持紧凑。
- 点击技能区标题即可展开或收起；展开后可以继续勾选当前模式技能并打开技能管理器。
- 每次软件启动恢复默认折叠状态，单图与漫画分镜已勾选的技能仍按原规则持久化。
- 右上角技能列表与技能添加、编辑、删除功能保持不变。

## 交互与兼容性

- 使用原生可访问折叠控件，支持鼠标、触控和键盘操作。
- 标题点击区域不小于 44px，提供展开方向、悬停和键盘焦点反馈。
- 展开箭头使用 180ms 过渡，并遵循系统的减少动态效果设置。
- Web 与 Android 内置页面资源保持一致。

## 验证

- 完整浏览器回归通过，覆盖默认折叠、点击展开及重启后恢复默认折叠状态。
- 静态审计、Android 资源哈希、`flutter analyze` 通过。
- Flutter 测试 81 项通过，1 项打包进程环境测试按条件跳过。
