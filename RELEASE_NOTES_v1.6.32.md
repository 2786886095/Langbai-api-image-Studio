# AI 图片生成器 v1.6.32

## 全失败手动重试修复

- 所有供应商、所有错误类型现在都可以单张手动重试，也都会进入“全部失败重试”。
- 修复 HTTP 504、401、明确参数错误、内容审核、供应商界面能力错误和任务丢失等失败项被排除或按钮无响应的问题。
- 兼容旧项目保存的阻断标记，恢复项目后无需重新创建分镜即可重试。
- 用户点击“全部失败重试”后，每个失败项至少执行一次；一旦真实返回图片，该项立即停止重试。

## 安全边界

- 手动重试代表用户明确重新提交请求；对于结果未知的 HTTP 504，仍可能产生重复任务或重复计费。
- 自动追加轮次继续遵循原有安全策略，不会在后台盲目重复未知结果的 504、鉴权错误或明确无效参数。
- 参数错误和审核拦截的单张重试仍会先打开编辑提示词界面。

## 兼容性与验证

- Gemini 本地去水印、API 配置、账号、历史项目、缓存、导出与局部重绘保持不变。
- 新增六类失败的手动重试契约测试，并覆盖旧版阻断标记恢复。
- 完整浏览器回归、静态审计、重试策略单测、`flutter analyze` 与 Flutter 全量测试通过。
