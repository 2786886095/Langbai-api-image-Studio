# ChatGPT 网页生图接入说明

## 当前入口

Langbai 的 Windows 本地生图入口只保留 **ChatGPT 网页生图**：

- 固定地址：`http://127.0.0.1:18081/v1`
- 固定模型：`gpt-image-2`
- 使用当前 Edge 中已登录的 ChatGPT 网页会话
- 不再提供 Codex 路径和 Codex/网页混合切换入口

官方 OpenAI、GrsAI、自定义 API 仍作为彼此独立的供应商保留。

## 并发

- “同时生成数”允许输入 `1–100`
- 默认值为 `10`
- 每个上游请求保持 `n=1`
- 网关最多同时执行 100 个任务，超出执行槽的任务保持 `queued`
- 高并发并不代表上游保证相同吞吐；网页端限流、审核、账号状态仍由 ChatGPT 决定

## 参考图与任务恢复

- 每个请求最多接收 20 张 PNG/JPEG/WebP Data URL 参考图
- 使用可轮询的异步任务 ID
- 软件重启或网络短暂中断后恢复轮询，不重复提交已经创建的任务
- 本地应用密钥只通过 Windows 安全桥加载到内存，不保存到 API 配置或历史记录

## 尺寸模式

- `native`：保留上游实际尺寸
- `strict_native`：实际尺寸与请求不符时报告尺寸不符
- `exact_output`：生成后进行无拉伸覆盖裁切和 Lanczos 缩放，输出精确目标像素

## 验证

浏览器回归必须确认：

1. 页面不存在 Codex/ChatGPT 路由切换控件；
2. 本地网关固定使用 `18081`；
3. 并发输入默认 10，边界值正确限制到 1 和 100；
4. 生成、参考图、异步续传、受保护图片下载与历史压缩均通过；
5. 官方 OpenAI、GrsAI、自定义 API 入口仍存在。
