# ChatGPT 网页额度生图路由实施说明

## 软件改动

Langbai 的“Codex 专用生图网关”新增独立的“额度来源”选项：

- `Codex`：保持原来的 `http://127.0.0.1:18080/v1`；
- `ChatGPT 网页`：切换到 `http://127.0.0.1:18081/v1`。

两个路由继续复用 Windows 原生层提供的本机应用密钥。密钥只进入运行内存，不写入软件配置、
历史记录或项目导出文件，因此这次接入不需要新增原生桥操作，也不依赖重新编译 Windows 外壳。

## 稳定性约束

- 网页额度模式强制每个请求 `n=1`；
- 客户端并发自动限制为 `1`，降低网页账号限流和重复提交风险；
- 生图使用异步任务 ID，软件断线或重启后继续查询已有任务；
- 健康检查地址根据当前额度来源自动切换；
- 切换额度来源时清除内存凭据和能力缓存并重新检测；
- 参考图继续使用现有 JSON Data URL 协议，最多接收 20 张；
- 所有本机 127.0.0.1 请求继续绕过桌面代理。

## 尺寸策略

- `原生`：保留上游实际像素；
- `严格原生`：上游尺寸不等于请求尺寸时标记尺寸不符；
- `精确输出`：网关无拉伸覆盖裁切并缩放到目标像素。

网页 GPT Image 2 的原生结果按最高 2K 对待。软件请求 4K 并选择“精确输出”时，
最终文件可精确到 `3840×2160` 等目标尺寸，但属于本地后处理，不标注为原生 4K。

## 验收项目

- 浏览器完整交互回归通过；
- Flutter Analyze 通过；
- Flutter 测试 19/19 通过；
- 已安装 Windows 软件的 `app.js`、`index.html`、`codex-image-gateway.js`
  与源码 SHA-256 一致；
- 18081 网关健康检查和能力检查通过；
- Edge 会话扩展加载后，以 `session_available=true` 作为网页额度可用条件。
