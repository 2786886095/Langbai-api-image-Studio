# AI 图片生成器 v1.6.28

## 技能版块

- 新增右上角技能列表与左侧技能勾选区，支持功能类、风格类技能。
- 用户可以添加、编辑和删除技能，设置名称、提示词模板以及单图、漫画分镜或两者的适用范围。
- 单图与漫画分镜的启用状态相互独立并持久化，多个技能按照列表顺序注入提示词。
- 内置“精致二次元赛璐璐上色”风格技能，默认关闭，由用户手动勾选。

## 角色三视图生成

- 原嵌字模式替换为角色三视图生成，嵌字编辑、批量嵌字及对应运行时代码已清理。
- 支持批量导入角色图片，按文件名自然排序；每张图片可以填写独立角色说明。
- 每张图片作为单独任务并且只携带自身参考图，避免批量参考图集中提交。
- 输出固定为 16:9，严格生成白色背景中的正面、90°侧面、背面三个全身视图。
- 提示词要求保留角色面部、发型、体型、服装、配饰、配色、材质、线稿与渲染风格，不添加文字、标记、水印或额外视图。
- 三视图模式不注入单图或漫画技能，支持历史记录、失败重试、项目保存、导出与恢复。

## 验证

- JavaScript 语法、静态审计、API 代理、ChatGPT/Gemini 网关、任务稳定性与 Service Worker 测试通过。
- 完整浏览器回归通过，覆盖技能 CRUD、作用域、持久化、注入顺序、三视图批量请求、重试与恢复。
- `flutter analyze` 通过；Flutter 测试 81 项通过，1 项打包进程环境测试按条件跳过。
