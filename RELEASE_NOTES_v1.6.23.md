# AI 图片生成器 v1.6.23

## Gemini 本地去水印

- Gemini 网页生图结果默认在本机完成去水印后再进入预览、缓存、历史和下载。
- 设置中新增“Gemini 去水印”开关，默认开启；关闭后保留网页下载的原始文件字节。
- 顶部新增橡皮擦入口，可导入单张或多张 PNG、JPEG、WebP 图片进行本地处理。
- 单张图片直接保存到用户选择的目录；多张图片自动创建 `gemini-watermark-removed-*` 文件夹。
- 输出统一为 PNG，并保持输入像素尺寸，不执行裁切、缩小或放大。

## 工程与审计

- 内置 `@pilio/gemini-watermark-remover` 1.0.33 浏览器处理核心，运行时不需要独立服务或外部去水印 API。
- 补充上游 MIT 许可证、第三方来源说明、Flutter 资源、Service Worker 离线缓存和 Android 资源同步检查。
- 生成结果审计记录处理状态、模板尺寸、命中位置、决策层级、处理次数及原始/最终 SHA-256。
- 新增 Gemini 去水印集成回归测试和真实浏览器 Canvas 冒烟验证。

## 升级说明

- 保留已有 API 配置、Gemini/ChatGPT 账号、历史项目、缓存、下载目录和常用尺寸。
- 从 v1.6.22 及更早版本升级后，自动去水印按默认开启生效，可随时在设置中关闭。
