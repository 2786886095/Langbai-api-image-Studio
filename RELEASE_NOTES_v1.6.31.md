# AI 图片生成器 v1.6.31

## 全部失败重试修复

- 修复 `HTTP 400: generate image failed` 被统一误判为参数错误，导致“全部失败重试”不执行的问题。
- 通用 HTTP 400 现在按照用户设置执行有限重试，并在成功返回图片后立即停止。
- 明确的参数错误、内容审核、认证错误仍保持隔离，避免无效的原样重复提交。
- 当所有失败项都需要修改时，按钮保持可点击，显示可重试数/失败总数，并自动定位、高亮第一个待编辑分镜。

## 多端界面修复

- 修复 Android 与 iOS 顶栏语言和功能按钮横向越界。
- 修复桌面窄输入栏与移动端分镜表需要横向滚动、重试/参考图/删除操作被隐藏的问题。
- Windows、macOS、Android、iOS 的深色和浅色界面均完成 Headless 截图审计；主界面、分镜区和设置弹窗无页面横向溢出或可见控件越界。

## 兼容性与验证

- 保留 Gemini 本地去水印、API 配置、历史项目、缓存、导出、局部重绘和多供应商隔离逻辑。
- Node 单元测试 24 项通过，完整浏览器交互回归通过。
- `flutter analyze` 无问题；Flutter 测试 81 项通过，1 项打包进程测试按环境跳过。
- Android 内嵌 Web 资源与主资源一致。
