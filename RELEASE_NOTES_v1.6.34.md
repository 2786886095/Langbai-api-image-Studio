# AI 图片生成器 v1.6.34

## 导出完整性修复

- 修复“保存到文件夹”时随机遗漏部分已成功图片，但界面仍显示保存完成的问题。
- 导出会优先使用生成时保留的本地图片字节，其次读取持久缓存，最后尝试主地址和备用地址。
- 临时读取失败最多自动重试 3 次；持续失败会明确显示对应分镜并中止导出。
- 所有图片都通过预检后才开始写入文件夹，避免图片源在导出中途失效造成静默缺图。
- 磁盘写入失败会停止后续流程，显示已保存数量与失败分镜，不再写入误导性的项目清单。

## ZIP 导出

- ZIP 采用和文件夹保存相同的完整性校验与有限重试。
- 任意图片无法取得完整字节时不创建残缺 ZIP，也不再用 `download-errors.txt` 掩盖缺图。
- 项目提示词、参考图、`project.json`、审计数据和联系表的原有结构保持不变。

## 验证

- 浏览器完整交互回归全部通过，新增临时失败后恢复、持续失败拒绝残缺 ZIP、文件写入失败停止清单写入测试。
- 静态发布审计、JavaScript 语法检查、`flutter analyze` 与 Flutter 全量测试通过。
- Android 内嵌前端资源已同步。
- Gemini 本地去水印、API 配置、历史项目、重试队列和缓存逻辑保持不变。
