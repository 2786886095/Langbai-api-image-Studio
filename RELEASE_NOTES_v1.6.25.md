# AI 图片生成器 v1.6.25

## ChatGPT 失败任务可重试

- 认证或账号权限失败的卡片不再保留一个无响应的“重试”按钮。
- Windows 点击“重试”会打开重新登录窗口；登录成功后自动继续原失败任务。
- Android 点击“重试”会打开账号设置并聚焦 Session 导入；导入成功后自动继续原失败任务。
- ChatGPT 本机桥接超时允许手动与批量重试。

## 本机网关恢复

- 相同账号和令牌在同一网关进程中只激活一次，减少并发任务重复写入会话造成的阻塞。
- 会话桥接超时时自动重启内置网关一次，重新写入当前账号后继续原请求。
- 网关启动、账号导入、账号切换与激活的原生桥接等待时间覆盖完整恢复窗口。

## 验证与兼容

- 新增真实按钮点击回归：401 → 重新登录 → 自动续跑原卡片。
- 新增 `TimeoutException after 0:00:10.000000` 失败卡片的手动与批量重试回归。
- 保留 v1.6.24 的 ChatGPT 配置持久化、网关熔断恢复，以及 v1.6.23 的 Gemini 本地去水印与批量处理功能。
